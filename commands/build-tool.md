---
description: Build a new reusable tool (CLI, MCP server, justfile recipe, pipeline, or script) and register it in the tool index
argument-hint: [description of the tool to build]
---
Invoke the building-reusable-tools skill for: $ARGUMENTS

After the tool is built and verified, register it using `/tool-index add` and sync to the global catalog with `/tool-index sync`.
